<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WaiterApp4.ViewModels"
             mc:Ignorable="d"
             x:Class="WaiterApp4.Pages.OrderPage">


    <ContentPage.BindingContext>
        <local:OrderViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout VerticalOptions="StartAndExpand">
                <StackLayout HeightRequest="40">
                    <Label Text="Bord"
                           FontSize="26"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center"
                           BackgroundColor="Blue"
                           HorizontalOptions="FillAndExpand"
                           HeightRequest="40"
                           />
                </StackLayout>
                <StackLayout>
                    <CollectionView ItemsSource="{Binding Order}" SelectionMode="Single" SelectionChanged="CollectionView_SelectionChanged" Margin="5">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Vertical" Span="2" VerticalItemSpacing="3" HorizontalItemSpacing="2" />
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout BackgroundColor="LightSteelBlue" HeightRequest="180" VerticalOptions="StartAndExpand">
                                    <Label
                                        Text="{Binding Name}"
                                        FontAttributes="Bold"
                                        FontSize="24"
                                        VerticalOptions="Start"
                                        HorizontalOptions="Center"/>
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                    <StackLayout>
                        <ListView ItemsSource="{Binding Order:Orderlines}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Label 
                                    Text="{Binding FullOrderline}"
                                    />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </StackLayout>
                <StackLayout>
                    <Button Text="CONFIRM" FontSize="36" BackgroundColor="Blue" HorizontalOptions="FillAndExpand"  HeightRequest="80" Clicked="Button_Clicked"/>
                </StackLayout>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>